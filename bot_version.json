{
  "version": "2.0.2",
  "codename": "Phoenix",
  "lastUpdated": "2026-09-20T00:00:00.000Z",
  "changelog": [
    {
      "type": "fixed",
      "description": "Pairing code never displayed - was requested before socket ready (race condition)"
    },
    {
      "type": "fixed",
      "description": "Pairing failure now falls back to QR code instead of hanging"
    },
    {
      "type": "fixed",
      "description": "edbots update now works on npm installs, not just git clones"
    },
    {
      "type": "fixed",
      "description": "edbots update uses native fetch (no axios dependency at boot)"
    },
    {
      "type": "added",
      "description": "Connection loop limit (10 attempts) to prevent infinite reconnects"
    }
  ]
}
